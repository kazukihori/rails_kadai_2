<h1>登録済み一覧</h1>
<table>
<tr>
<th>施設画像</th><th>施設名</th><th>施設紹介</th><th>料金</th><th>作成日</th>
</tr>
<% @rooms.each do |room| %>
<tr>
<td><%= image_tag room.room_img.url%></td><td><%= room.name %></td><td><%= room.room_introduction %></td><td><%= room.price %></td><td><%= room.created_at.strftime('%Y/%m/%d')  %></td><td><%= link_to "編集" , "/rooms/#{room.id}/edit" %></td>
</tr>
</table>
<% end %>
